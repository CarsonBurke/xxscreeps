---
'xxscreeps': patch
---

Ship the engine documentation with the package. The repository README now covers the reinforcement-learning stack that drives the engine, so the engine's own concepts, setup, bot management, CLI, scripting and Docker instructions live in `packages/xxscreeps/README.md` and reach npm with the package.
