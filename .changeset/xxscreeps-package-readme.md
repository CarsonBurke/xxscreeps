---
'xxscreeps': patch
---

Give the package its own README. The repository README now covers the reinforcement-learning stack that drives the engine, so the engine's concepts, setup, bot management, CLI, scripting and Docker instructions live in `packages/xxscreeps/README.md`. Its links are repository-relative, which is what the fork's readers follow; the package is not published from this fork.
