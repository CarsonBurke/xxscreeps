[project]
name = "screeps-rl"
version = "0.1.0"
description = "Colony-scale PPO agent for Screeps, trained against the xxscreeps engine"
requires-python = ">=3.10"
dependencies = [
  "torch>=2.1",
  "numpy>=1.24",
  "tensorboard>=2.14",
]

[project.optional-dependencies]
metrics = ["polars>=0.20", "pyarrow>=14"]

# Repository commands use `python3 -m samples.rl.agent...`; the installed wheel
# exposes the discovered top-level `agent` package through these console scripts.

[project.scripts]
screeps-rl-train = "agent.train:main"
screeps-rl-joint-pretrain = "agent.pretrain_joint:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = []

[tool.setuptools.packages.find]
include = ["agent*"]
